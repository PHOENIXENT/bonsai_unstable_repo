def generate_scenario(prompt="Create a sunset scene with a robot dancing."):
    # For simplicity, a hard-coded response here
    # In reality, you'd use an AI model or prompt-based system
    scenario = {
        "environment": "sunset",
        "objects": [
            {"type": "robot", "action": "dancing"},
            {"type": "tree", "count": 5}
        ],
        "lighting": "sunset glow",
    }
    return scenario

    